# 考纲
1. Spring Boot（难度：普通 · 理解 · 2% / 困难 · 应用 · 4%）
   Spring Boot ⇒ Spring AOP ⇒ AOP开发 ⇒ 通知获取参数
   Spring Boot ⇒ Spring AOP ⇒ AOP开发 ⇒ 织入
   Spring Boot ⇒ 数据库事务处理 ⇒ @Transactional自调用
   Spring Boot ⇒ Spring Security ⇒ 安全认证
   Spring Boot ⇒ Spring Security ⇒ 限制请求 ⇒ 访问权限
   Spring Boot ⇒ Spring Security ⇒ 用户认证
   Spring Boot ⇒ Spring高级 ⇒ 异步线程池 ⇒ 异步方法
   Spring Boot ⇒ Spring高级 ⇒ 异步消息

2. Spring Boot（难度：困难 · 应用 · 2%）
   Spring Boot ⇒ 数据库事务处理 ⇒ Spring声明式事务的使用 ⇒ Spring事务管理器
   Spring Boot ⇒ Spring Security ⇒ 自定义用户服务信息 ⇒ 内存用户认证服务
   Spring Boot ⇒ Spring Security ⇒ 自定义用户服务信息 ⇒ 数据库定义用户认证服务
   Spring Boot ⇒ Spring Security ⇒ 自定义用户服务信息 ⇒ 用户认证服务
   Spring Boot ⇒ Spring Security ⇒ 限制请求 ⇒ 请求伪造

3. SpringMVC（难度：困难 · 应用 · 2%）
   SpringMVC => 框架使用 => 常用注解 => @Controller
   SpringMVC => 框架使用 => 常用注解 => @RequestMapping
   SpringMVC => 框架使用 => 常用注解 => @PathVariable
   SpringMVC => 框架使用 => 常用注解 => @RequestParam
   SpringMVC => 框架使用 => 常用注解 => @RequestBody
   SpringMVC => 框架使用 => 常用注解 => @ResponseBody
   SpringMVC => 框架使用 => 常用注解 => @ModelAttribute
   SpringMVC => 框架使用 => 常用注解 => @ExceptionHandler
   SpringMVC => 框架使用 => 常用注解 => @ControllerAdvice
   SpringMVC => 框架使用 => Rest => Restful 请求方式
   SpringMVC => 框架使用 => Rest => 统一响应结构
   SpringMVC => 框架源码 => 启动流程 => ContextLoaderListener 初始化
   SpringMVC => 框架源码 => 启动流程 => Filter 初始化
   SpringMVC => 框架源码 => 启动流程 => DispatcherServlet 初始化
   SpringMVC => 框架源码 => 启动流程 => Servlet 生命周期
   SpringMVC => MVC模式 => 什么是MVC
   SpringMVC => Spring框架 => Spring框架的优势
   SpringMVC => Spring框架 => IoC
   SpringMVC => Spring框架 => AOP
   SpringMVC => 控制器 => 控制器相关注解
   SpringMVC => 控制器 => 控制器返回结果
   SpringMVC => 统一异常处理 => 相关注解
   SpringMVC => 注解 => 注解概念理解

4. SpringMVC（难度：困难 · 应用 · 2%）
   SpringMVC => 框架使用 => 过滤器拦截器
   SpringMVC => 框架使用 => Rest => 对象序列化
   SpringMVC => 框架使用 => Rest => 统一异常处理
   SpringMVC => 拦截器 => 拦截器原理
   SpringMVC => 拦截器 => 自定义拦截器

5. SpringMVC（难度：困难 · 应用 · 2%）
   SpringMVC => 框架源码 => 核心组件 => DispatcherServlet
   SpringMVC => 框架源码 => 核心组件 => HandlerMapping
   SpringMVC => 框架源码 => 核心组件 => HandlerAdapter
   SpringMVC => 框架源码 => 核心组件 => ViewResolver
   SpringMVC => 框架源码 => 核心组件 => Handler
   SpringMVC => 框架源码 => 设计模式 => 责任链模式
   SpringMVC => 框架源码 => 设计模式 => 适配器模式
   SpringMVC => 框架源码 => 设计模式 => 策略模式
   SpringMVC => 框架源码 => 设计模式 => 建造者模式
   SpringMVC => 框架源码 => 设计模式 => 组合模式

6. Spring Cloud（难度：普通 · 理解 · 4% / 困难 · 应用 · 4%）
   Spring Cloud => 断路器 => Hystrix组件
   Spring Cloud => 断路器 => Sentinel组件
   Spring Cloud => API网关服务 => zuul组件
   Spring Cloud => API网关服务 => zuul2组件
   Spring Cloud => 负载均衡 => Ribbon组件
   Spring Cloud => 负载均衡 => LoadBalancer组件
   Spring Cloud => 构建与部署 => Kubernetes
   Spring Cloud => Spring Cloud Alibaba => 服务治理
   Spring Cloud Alibaba => 负载均衡
   Spring Cloud Alibaba => Sentinel
   Spring Cloud Alibaba => 配置中心

7. MyBatis（难度：普通 · 理解 · 2% / 困难 · 应用 · 4%）
   MyBatis => 缓存 => 系统缓存
   MyBatis => 动态sql => bind用法
   MyBatis => MyBatis运行原理 => SqlSessionFactory构建过程
   MyBatis => MyBatis运行原理 => SqlSession运行过程

8. MyBatis（难度：普通 · 理解 · 2% / 困难 · 应用 · 2%）
   MyBatis => 缓存 => 自定义缓存

9. 中间件（难度：困难 · 应用 · 2%）
   中间件 => 主流中间件 => Kafka => 核心功能
   中间件 => 主流中间件 => Redis => 核心功能
   中间件 => 主流中间件 => Elasticsearch => 应用场景

10. 中间件（难度：简单 · 记忆 · 2%）
    中间件 => 主流中间件 => RabbitMQ => 高级功能

11. 数据库原理（难度：困难 · 应用 · 4%）
    数据库原理 => SQL => 数据定义 => 主键和外键
    数据库原理 => SQL => 数据定义 => 索引

12. 数据库原理（难度：困难 · 应用 · 4%）
    数据库原理 => 关系数据库理论
    数据库原理 => 规范化 => 1NF
    数据库原理 => 规范化 => 2NF
    数据库原理 => 规范化 => 3NF
    数据库原理 => 规范化 => BCNF
    数据库原理 => 并发控制 => 封锁
    数据库原理 => 并发控制 => 活锁和死锁
    数据库原理 => 并发控制 => 调度的可串行性
    数据库原理 => 并发控制 => 两段锁协议

13. 数据库（难度：困难 · 应用 · 4%）
    数据库 => 查询方式 => 多表查询
    数据库 => 查询方式 => 子查询
    数据库 => 查询方式 => 专有查询
    数据库 => 查询方式 => 条件查询
    数据库 => 基本对象 => 索引
    数据库 => 基本对象 => 约束
    数据库 => 表的常用操作 => 创建
    数据库 => 表的常用操作 => 修改
    数据库 => 表的常用操作 => 删除
    数据库 => 条件查询 => where
    数据库 => 条件查询 => like
    数据库 => 条件查询 => null
    数据库 => 条件查询 => between and
    数据库 => 条件查询 => limit
    数据库 => 条件查询 => order by
    数据库 => 聚合 => count
    数据库 => 聚合 => max/min
    数据库 => 聚合 => sum/avg

14. 数据库（难度：困难 · 应用 · 4%）
    数据库 => 基本对象 => 分区
    数据库 => 表的常用操作 => 表设计
    数据库 => 字段约束 => 主键约束
    数据库 => 字段约束 => 主键必增策略
    数据库 => 字段约束 => 非空约束
    数据库 => 字段约束 => 唯一约束
    数据库 => 分组 => group by
    数据库 => 分组 => having

15. Kafka（难度：普通 · 理解 · 2% / 困难 · 应用 · 4%）
    Kafka => 体系结构 => zookeeper
    Kafka => 体系结构 => broker
    Kafka => 消息管理 => 消息

16. Kafka（难度：普通 · 理解 · 4%）
    Kafka => Kafka基础 => 架构
    Kafka => 体系结构 => 分区
    Kafka => 体系结构 => 主题
    Kafka => 消息管理 => 数据
    Kafka => 消息管理 => 日志
    Kafka => 消息管理 => 异常

17. SQL（难度：普通 · 理解 · 2%）
    SQL => 聚合与排序 => 分组
    SQL => 聚合与排序 => HAVING子句
    SQL => 复杂查询 => 子查询
    SQL => 复杂查询 => 关联子查询
    SQL => 语言元素 => 事务
    SQL => 语言元素 => 变量

18. SQL（难度：困难 · 应用 · 4%）
    SQL => 聚合运算 => 联结
    SQL => 高级应用 => 执行事务

19. 架构设计（难度：困难 · 应用 · 8%）
    架构设计 => 接入层 => 数据安全
    架构设计 => 接入层 => 设计
    架构设计 => 业务逻辑层 => 设计 => 服务降级
    架构设计 => 业务逻辑层 => 实践
    架构设计 => 分布式缓存 => 缓存类型
    架构设计 => 分布式缓存 => 缓存冗余
    架构设计 => 堆栈 => Java 线程堆栈实践
    架构设计 => 进程管理 => PV操作
    架构设计 => 进程管理 => 死锁
    架构设计 => 网络 => HTTP => 状态码
    架构设计 => 高并发 => 缓存 => 有效性
    架构设计 => 高并发 => 缓存 => 雪崩
    架构设计 => 高并发 => 多线程 => volatile
    架构设计 => 高并发 => 多线程 => synchronized
    架构设计 => 架构设计 => 微服务
    架构设计 => 架构设计 => 部署
    架构设计 => 架构设计 => 设计原则
    架构设计 => 架构设计 => API => REST => 谓词
    架构设计 => 业务逻辑层 => 设计 => 逻辑层 => 事务控制
    架构设计 => 业务逻辑层 => 设计 => 逻辑层 => 重要设计原则

# SpringCloud
## 核心概念
Zuul = 大门门卫（网关，统一入口）  
字面：祖尔（印度神话里地狱守门恶鬼、门禁守护者）  
命名寓意：网关作为请求的大门，拦截、管控所有流量。  

Sentinel = 巡逻哨兵（实时流量监控防护）  
字面：哨兵、守卫、岗哨  
命名寓意：像哨兵实时监控接口流量，一旦异常立刻拦截限流、熔断防护。  

Hystrix = 针对 “系统崩溃” 问题的防护工具  
词根来自 hysteria，字面：歇斯底里、失控崩溃  
命名寓意：组件用来防止服务调用链 “失控雪崩”，阻止系统陷入崩溃。

Ribbon：丝带 → 串联服务做负载均衡
字面：丝带、缎带、带状捆束
命名寓意：把多个服务实例 “串联绑定” 起来，做客户端负载均衡，像丝带一样串联所有可用节点。

OpenFeign：模拟伪装 → 伪装成本地接口远程调用  
Feign：假装、伪装、模拟  
Open：开源开放  
命名寓意：伪装成本地接口，开发者像调用本地方法一样远程调用服务，屏蔽底层 HTTP 网络细节。

Kubernetes：船长 / 舵手 → 容器集群调度管理者  
词源来自希腊语 κυβερνήτης，字面：舵手、领航员、船长  
命名寓意：掌控、调度所有容器，像船长指挥整艘船，统一管理集群内所有微服务容器。  
行业小知识：中间 8 个字母 ubernetes 省略，简写 K8s。  

### 一、Zuul（Spring Cloud 网关，你的强项）
#### 核心定位
Spring Cloud 第一代API网关，基于**同步阻塞Servlet**实现，负责请求统一入口、路由分发、鉴权、限流、日志、灰度发布。
#### 核心机制
1. 四大过滤器生命周期：`PRE`前置、`ROUTE`路由转发、`POST`响应处理、`ERROR`异常拦截，可自定义业务逻辑。
2. 整合了Ribbon做服务负载均衡，整合Hystrix实现路由熔断。
3. 缺陷：同步阻塞模型，Tomcat线程池有限，高并发场景易线程耗尽、吞吐量低，**Spring Cloud官方已停止迭代，推荐Gateway替代**。
#### 适用场景
老旧微服务架构兼容、简单轻量网关需求；高并发、大流量项目不推荐。

### 二、Spring Cloud LoadBalancer（客户端负载均衡，你的强项）
#### 核心定位
Spring Cloud官方推出，**Ribbon替代方案**，客户端侧负载均衡组件，脱离Netflix体系，原生适配SpringBoot3/新版云组件。
#### 核心机制
1. 从注册中心（Nacos/Eureka）拉取服务实例列表，本地内存缓存。
2. 内置轮询、随机策略，支持**全局/单服务自定义均衡规则**（本地优先、权重、最小连接数）。
3. 无缝适配OpenFeign、RestTemplate，无需额外改造；无独立线程池，轻量化性能损耗较小。
#### 和Ribbon关键区别
Ribbon属于Netflix闭源组件，停止维护；LoadBalancer是Spring官方自研，持续更新、适配云原生K8s。

### 三、Hystrix（服务熔断降级，你的弱项）
#### 核心定位
Netflix开源分布式服务容错组件，通过**线程池隔离+滑动窗口熔断**解决微服务调用雪崩问题。
#### 核心机制
1. 线程池隔离：不同下游服务分配独立线程池，一个服务故障不会拖垮全部调用。
2. 熔断三状态：关闭（正常放行）→打开（直接走降级逻辑）→半开（少量试探请求自动恢复）。
3. 滑动窗口统计超时、异常比例，超过阈值触发熔断；通过`@HystrixCommand`注解实现降级方法。
#### 致命短板
1. 基于AOP代理，同类`this`自调用注解失效；
2. 项目早已停止维护，无新特性、不支持云原生集群限流；
3. 能力单一，仅熔断降级，无精细化热点限流、实时监控面板。

### 四、Sentinel（流量防护，你的弱项）
#### 核心定位
阿里开源新一代分布式流量控制组件，全方位流量防护，是Hystrix生产级替代品。
#### 核心能力（远超Hystrix）
1. 多维度管控：QPS限流、线程数限流、**热点参数限流**、集群限流、链路限流；
2. 熔断降级：支持异常比例、慢调用比例、异常数三种熔断策略；
3. 自带可视化Dashboard控制台，实时监控接口流量、异常；
4. 规则动态推送，修改规则无需重启服务；支持SPI自定义限流逻辑。
#### 和Hystrix对比
Hystrix侧重故障隔离；Sentinel兼顾限流、熔断、系统自适应保护，云原生项目首选。

### 五、OpenFeign（声明式远程调用，你的弱项）
#### 核心定位
Spring Cloud声明式HTTP调用框架，封装HTTP请求，简化微服务之间远程接口调用。
#### 核心机制
1. 基于接口+注解定义远程API（`@FeignClient`指定服务名），无需手动拼接URL、封装HTTP工具；
2. 新版自动整合LoadBalancer实现客户端负载均衡；
3. **原生无熔断能力**，必须手动整合Hystrix/Sentinel才能实现降级；
4. 默认集成Jackson序列化，支持自定义编码器、解码器、拦截器。
#### 高频踩坑考点
1. 接口参数传递需注意`@RequestParam`/`@RequestBody`注解规范，否则参数丢失；
2. 同类内部调用Feign接口不会触发负载均衡；
3. 超时时间分Feign连接超时、读超时两层配置。

### 六、Kubernetes(K8s，容器编排，你的强项)
#### 核心定位
云原生标准容器编排平台，自动化管理Docker容器，实现微服务部署、扩缩容、自愈、滚动更新、资源调度。
#### 核心核心资源对象
1. Pod：最小调度单元，运行业务容器；本地存储临时数据，销毁丢失；持久化依赖PV/PVC。
2. Deployment：管理无状态微服务，实现滚动发布、副本扩缩容、故障自动重启。
3. Service：固定访问入口，屏蔽Pod动态IP，内置四层负载均衡，供网关/服务内部调用。
4. ConfigMap/Secret：统一管理配置文件、环境变量、加密密钥。
#### 微服务场景价值
1. 自动扩缩容：流量高峰自动增加Pod实例，低谷缩容节约资源；
2. 自愈：Pod异常崩溃，平台自动重建；
3. 灰度/蓝绿发布：零停机更新微服务版本；
4. 资源隔离：CPU、内存配额限制，避免服务资源抢占。

# Spring
### Spring 事务管理器
#### 题目
项目多数据源场景，分别配置 DataSource1、DataSource2，对应两个事务管理器txManager1、txManager2；在方法上使用@Transactional(value = "txManager2", rollbackFor = Exception.class)，下列说法错误的是？  
A. value 指定当前方法使用 txManager2 管理事务  
B. 若省略 value，会使用容器中默认标记@Primary的事务管理器  
C. rollbackFor = Exception.class 时，除非捕获异常，否则所有受检、非受检异常都会触发回滚  
D. 不配置 rollbackFor 时，默认 RuntimeException 和 Error 触发回滚，捕获 Exception 不会回滚
#### 正确答案
C
#### 简洁解析
A 正确：@Transactional 的 value 属性绑定指定事务管理器。  
B 正确：多事务管理器必须指定 value，无指定则取 @Primary 主管理器。  
C 错误：rollbackFor=Exception.class 抛出异常时回滚；若代码 try-catch 捕获 Exception，不会触发回滚。  
D 正确：Spring 默认回滚规则是 `RuntimeException` 和 `Error` 触发回滚，受检异常不会触发；若捕获 Exception 且未重新抛出，也不会回滚。 
#### 考点总结
- 单层事务、仅 try-catch 吞异常、无手动标记：不回滚；
#### 延伸考点
- catch 内调用setRollbackOnly()：捕获异常也回滚；
- REQUIRED 嵌套事务，子方法抛异常：外层捕获依然回滚；（因为内外层事务标记是共享的，一旦内层打上 rollback-only，整个事务再也无法提交）
- Spring 默认事务传播级别：Propagation.REQUIRED，默认隔离级别：Isolation.DEFAULT，使用底层数据库默认隔离级别；
- Spring 默认自动回滚范围：RuntimeException、Error。

### Spring 过滤器与拦截器
#### 题目  
关于 Filter 过滤器与 SpringMVC Interceptor 拦截器执行顺序、底层容器归属，说法正确的是？   
A. 拦截器先执行，过滤器后执行；二者都由Spring IoC容器管理  
B. Filter属于Servlet规范，由Tomcat容器管理；Interceptor由DispatcherServlet创建，归属Spring IoC  
C. 当 Interceptor 的 postHandle () 方法抛出异常时，所有拦截器的 afterCompletion () 都不会执行，Filter 后置逻辑也会中断
D. 过滤器无法注入Spring容器Bean，拦截器不能注入Servlet原生对象  
#### 正确答案
**B**
#### 简洁解析
1. A 错误：执行顺序Filter → Interceptor；Filter不归IoC管理。
2. B 正确：Filter是Servlet API组件，由Web容器创建；拦截器是SpringMVC组件，由IoC管理。
3. C 错误：postHandle 抛异常不会阻断 afterCompletion，所有成功放行的拦截器都会倒序执行 afterCompletion；Filter 后置逻辑独立于 MVC 流程，照常执行。
4. D 错误：Filter可通过代理/注册Bean注入IoC，拦截器也可获取Servlet相关对象。
#### 考点总结
- Filter 与 Interceptor 分属两套体系：Filter 基于 Servlet 规范，由 Web 容器管理；Interceptor 基于 Spring MVC，由 Spring IoC 容器管理。
- 执行顺序为 Filter → DispatcherServlet → Interceptor，且异常传播路径不同，拦截器内部异常不影响 Filter 的后置逻辑。
- Spring 的拦截器执行机制类似于一个 “环绕通知” 的链式调用：
  - preHandle 决定是否“进门”（返回 true 才进门）。
  - 只有“进门”了，afterCompletion 才会被放入一个 finally 块中等待执行。
  - postHandle 是在 try 块正常执行后的回调，而 afterCompletion 是在最终的 finally 块中执行，所以它永远能感知到当前线程抛出的最后一个异常（只要它被注册过）。
- Filter 可通过注册为 Spring Bean 的方式注入容器中的依赖，但其生命周期仍由 Web 容器管理。

### Filter、Interceptor、@ControllerAdvice 异常场景执行顺序  
#### 题目  
同一个请求链路中，自定义Filter、自定义HandlerInterceptor、@ControllerAdvice异常处理执行顺序，下列流程正确的是？  
A. Filter前置 → Interceptor preHandle → Controller报错 → @ControllerAdvice → Interceptor postHandle → Filter后置  
B. Filter前置 → Interceptor preHandle → Controller报错 → @ControllerAdvice → Interceptor afterCompletion → Filter后置  
C. Filter前置 → Interceptor preHandle → Controller报错 → Interceptor postHandle → @ControllerAdvice → Filter后置  
D. Interceptor preHandle最先执行，Filter全部逻辑在所有MVC组件之后  
#### 正确答案
**B**
#### 简洁解析
1. Filter由Tomcat最先执行前置；  
2. preHandle放行进入控制器，控制器异常进入@ControllerAdvice全局异常；  
3. 不会执行postHandle（仅正常返回才走），直接执行afterCompletion；  
4. 最后执行Filter后置逻辑。  
#### 考点总结
过滤器、拦截器完整执行链路，异常场景下postHandle不执行的底层规则。  

### Spring Ioc父子容器模型  
#### 题目  
基于Spring IoC父子容器模型，关于DispatcherServlet创建的子容器，说法错误的是？  
A. 子容器可直接获取父IoC容器中注册的Service、Dao Bean  
B. 根容器无法访问MVC子容器内的@Controller、拦截器Bean  
C. 拦截器注册在MVC子容器，因此拦截器内无法注入根容器的Service  
D. 多DispatcherServlet场景，每个Servlet拥有独立MVC子容器，互不共享控制器Bean  
#### 正确答案
**C**
#### 简洁解析
1. A 正确：子容器可向上查找父容器Bean。  
2. B 正确：双亲委派模型单向查找，父容器看不到子容器Bean。  
3. C 错误：子容器Bean天然能注入父容器所有业务Bean，拦截器可正常注入Service。  
4. D 正确：多个DispatcherServlet对应独立上下文，控制器隔离。  
#### 考点总结
Spring 父子容器是框架在 Web 场景下的一种容器层级设计：父容器承载业务层基础设施（如 Service、DAO），子容器承载 Web 层表现逻辑（如 Controller、拦截器）；子容器可向上访问父容器，父容器不可向下访问子容器。
该设计实现分层解耦、提升复用性，是传统 Spring MVC 的核心架构特征。 在 Spring Boot 中，该层级结构被简化，默认使用单一容器管理所有 Bean。

### 拦截器 preHandle 返回 false 的执行链路分析  
#### 题目  
开发自定义拦截器，重写preHandle返回false，下列现象描述正确的是？  
A. 控制器方法执行，不执行postHandle，直接执行afterCompletion  
B. DispatcherServlet不再继续后续处理器流程，不会进入Controller，afterCompletion依旧执行  
C. 所有Filter后置逻辑会被跳过，直接返回响应  
D. preHandle返回false时，全局@ControllerAdvice异常处理器会捕获拦截器阻断异常  
#### 正确答案
**B**
#### 简洁解析
1. A 错误：preHandle返回false直接截断，控制器完全不执行。  
2. B 正确：preHandle=false中断链路，跳过Controller与postHandle，但一定会执行afterCompletion做资源清理。  
3. C 错误：Filter是外层组件，后置逻辑不受拦截器返回值影响，正常执行。  
4. D 错误：preHandle主动阻断不属于异常，不会进入全局异常处理器。  
#### 考点总结
拦截器preHandle返回false的链路截断规则，区分postHandle、afterCompletion执行时机。  
#### 参考源码
```java
boolean applyPreHandle(HttpServletRequest request, HttpServletResponse response) throws Exception {
    HandlerInterceptor[] interceptors = getInterceptors();
    if (!ObjectUtils.isEmpty(interceptors)) {
        for (int i = 0; i < interceptors.length; i++) {
            HandlerInterceptor interceptor = interceptors[i];
            // 1. 正序执行每个拦截器的 preHandle
            if (!interceptor.preHandle(request, response, this.handler)) {
                // 2. 一旦有拦截器返回 false，立即执行清理工作
                triggerAfterCompletion(request, response, null);
                // 3. 返回 false，通知 DispatcherServlet 中断流程
                return false;
            }
            // 4. 记录最后一个成功执行的拦截器的索引
            this.interceptorIndex = i;
        }
    }
    return true;
}
```

#### Throwable 介绍
```text
Throwable（所有错误和异常的超类）
├── Error（系统级严重错误，程序无法处理，不应捕获）
│   ├── OutOfMemoryError（内存溢出）
│   ├── StackOverflowError（栈溢出）
│   ├── NoClassDefFoundError（类定义找不到）
│   └── ...（一般不捕获）
│
└── Exception（程序可处理的问题）
    ├── RuntimeException（非受检异常 / Unchecked，编译期不强制处理）
    │   ├── NullPointerException（空指针）
    │   ├── IllegalArgumentException（非法参数）
    │   ├── IndexOutOfBoundsException（下标越界）
    │   ├── ClassCastException（类型转换异常）
    │   └── ...（开发中常见，可避免）
    │
    └── 非RuntimeException（受检异常 / Checked，编译期必须 try-catch 或 throws）
        ├── IOException（I/O 异常）
        ├── SQLException（数据库异常）
        ├── FileNotFoundException（文件未找到）
        ├── ClassNotFoundException（类未找到）
        └── ...（必须显式处理）
```

# Kafka
### Zookeeper 管理的元数据
#### 题目
以下哪项不属于Zookeeper管理的Kafka元数据？  
A. Controller选举信息，包括当前Controller Broker ID  
B. Broker注册信息，包括Broker ID和网络地址  
C. 消费者组的offset位移信息  
D. Topic配置信息，包括分区数量和副本分配  

#### 答案
**C**

#### 精简解析
1. **A 属于ZK管理**
   Controller主节点选举信息持久化在ZK，集群依靠ZK完成主节点切换。

2. **B 属于ZK管理**
   Broker启动后在ZK注册节点，记录ID、IP端口，下线节点自动清除。

3. **C 不属于ZK管理**
   Kafka 0.9及以上版本，消费offset统一存内置主题`__consumer_offsets`，不再保存在ZK。

4. **D 属于ZK管理**
   Topic分区、副本数量与分配方案全部记录在ZK中。

#### 考点小结
传统ZK架构下，集群、Broker、Topic、Controller元数据由ZK维护；消费者偏移量由Kafka内部主题保存。

### Zookeeper 重新选举
#### 题目
Kafka在高可用环境中使用Zookeeper来管理集群状态。当Kafka集群中的控制器节点故障时，以下哪个过程会在Zookeeper的帮助下触发，以确保集群恢复正常？  
A. Kafka所有Broker自动重启，重新加入集群。  
B. 由Zookeeper重新选举一个新的控制器节点来承担控制器角色  
C. Zookeeper将所有现有分区的领导者切換到不同的Broker，以确保消息可用  
D. Kafka生产者和消费者立即停止所有消息操作，等待手动恢复控制器  

#### 正确答案
**B**

#### 简洁解析
1. **A错误**
   控制器故障不会让全部Broker重启，无该机制。
2. **B正确**
   控制器在ZK抢占临时节点，原控制器宕机后临时节点消失，其他Broker竞争创建节点，完成新控制器选举。
3. **C错误**
   分区leader切换是新控制器上线后执行的操作，不是ZK直接完成。
4. **D错误**
   集群会自动选新控制器，无需手动恢复，生产消费不会全部暂停。

#### 考点总结
# Kafka 选举机制类比表

| 公司层级 | Kafka 对应组件 | 管理对象 | 选举/指派机制 | 详细过程 |
|:---|:---|:---|:---|:---|
| **公司总部** | **ZooKeeper 集群** | 管理临时节点和 Watcher 通知 | 提供选举协调服务 | 总部前台有一块"领导公示板"（`/controller` 临时节点）。每位员工入职时都在前台登记，总部通过定时签到（会话心跳）检测员工是否在职。员工离职或失联超过时限，总部自动从公示板上擦除其名字，并通知所有等候的员工"重新竞选"。 |
| **部门领导** | **Controller（控制器）** | 管理 **Broker**（集群节点上下线、Topic 创建删除、元数据同步） | **Controller 选举**（ZooKeeper 临时节点竞争） | 所有员工（Broker）在入职时都跑到总部前台，抢着在"领导公示板"上贴自己的名字，**第一个贴上去的人成为部门领导（Controller）**。其他人贴不上，就在前台等候通知（注册 Watcher）。现任领导因失联被总部从公示板上擦除名字后，前台立刻通知所有等候的员工，大家**再次一拥而上抢着贴名字**，先贴上去的人成为新领导。 |
| **项目小组长** | **Partition Leader** | 管理 **Partition**（分区内数据的读写请求、Follower 数据同步、ISR 维护） | **Partition Leader 选举**（由 Controller 从 ISR 中指派） | 新部门领导上任后，第一件事是检查所有项目组（Partition），看哪些组的组长（Leader）位置空缺。对于空缺的组，领导查阅该组的**核心骨干名单（ISR，即与前任组长数据完全同步的组员）**，**按名单顺序指定第一个人当新组长**。随后领导在总部备案任命结果（更新 ZK 元数据），并发邮件通知全部门所有员工和外部客户更新通讯录。 |

### Kafka 系统结构
#### 题目
关于 Kafka 主题（Topic）、分区、副本、消息存储机制，说法错误的是？
A. 一个Topic由多个Partition组成，分区是消息读写最小并行单元
B. 同一分区内所有消息严格有序，不同分区之间无法保证全局消息有序
C. 新建Topic时指定副本数大于Broker数量，Topic会创建成功但部分副本无法分配，分区处于under-replicated状态
D. Topic删除后磁盘上对应分区日志文件会立刻同步清除，不会占用磁盘空间
#### 正确答案
D
#### 简洁解析
1. A 正确：分区是Kafka并行读写基础，Topic逻辑划分，分区物理存储。
2. B 正确：分区内消息追加写入有序，多分区无法全局有序。
3. C 正确：副本数超过broker总数，副本分配不完整，分区副本不足。
4. D 错误：删除Topic仅标记元数据删除，日志文件会根据`log.retention.*`策略延迟清理，不会立刻删除。
#### 考点总结
Topic主题生命周期、分区有序性、副本分配、日志删除延迟机制。  
  
```text
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              Kafka 集群 (3台Broker)                                  │
│                                                                                     │
│  ┌───────────────────────────────────────────────────────────────────────────────┐  │
│  │                            Topic: "order-events"                              │  │
│  │                                                                               │  │
│  │  ┌──────────────────────┐  ┌──────────────────────┐  ┌─────────────────────┐  │  │
│  │  │      Broker 1        │  │      Broker 2        │  │     Broker 3        │  │  │
│  │  │                      │  │                      │  │                     │  │  │
│  │  │      Partition 0     │  │      Partition 0     │  │     Partition 1     │  │  │
│  │  │      (Leader)        │  │      (Follower)      │  │     (Follower)      │  │  │
│  │  │                      │  │                      │  │                     │  │  │
│  │  │      Partition 2     │  │      Partition 1     │  │     Partition 2     │  │  │
│  │  │      (Follower)      │  │      (Leader)        │  │     (Leader)        │  │  │
│  │  └──────────────────────┘  └──────────────── ─────┘  └─────────────────────┘  │  │
│  │                                                                               │  │
│  └───────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                     │
│  说明：                                                                              │
│  1. Topic "order-events" 有 3 个 Partition，分布在不同 Broker 上实现并行处理             │
│  2. 每个 Partition 有多个副本 (其中 1个是 Leader，其余是 Follower)                       │
│  3. 同一 Partition 的 Leader 和 Follower 存储的数据内容完全一致 (ISR状态下)               │
│  4. 区别：物理位置不同、角色不同 (Leader可读写，Follower只同步)                            │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

#### 题目
Kafka 消息核心结构、消息投递语义相关描述正确的是？  
A. 消息key为空时，生产者会固定投递到分区0  
B. 消息offset由生产者生成，broker仅负责持久化存储offset  
C. 批量发送的多条消息共享同一个timestamp，无法单独设置单条消息时间戳  
D. 消息的header属于附加元数据，不参与分区路由计算，仅做业务透传   
#### 正确答案
D
#### 简洁解析
1. A 错误：key为空采用轮询分发，不会固定分区0。
2. B 错误：offset是broker写入日志时分配的自增编号，生产者不生成offset。
3. C 错误：每条ProducerRecord可独立设置timestamp，批量消息时间戳可各不相同。
4. D 正确：分区路由只根据key哈希计算，headers不影响分区分配，仅用于业务标记。
#### 考点总结
消息ProducerRecord结构、offset生成逻辑、key路由、消息header作用。

#### 题目
结合Zookeeper、Broker、Topic分区副本机制，下列说法错误的是？  
A. ZK存储Topic元数据、分区副本分配、控制器Broker信息，不存储消息数据  
B. 控制器Broker负责Topic分区leader副本切换，元数据变更同步写入ZK  
C. 创建Topic时，ZK直接持久化分区日志文件索引，加速消费者offset查询  
D. 分区ISR集合存储在ZK，leader副本只允许从ISR列表内选举新leader  
#### 正确答案
C
#### 简洁解析
1. A 正确：ZK只存元数据，所有消息日志全部存在Broker磁盘。
2. B 正确：集群控制器管理分区leader切换，元数据落地ZK。
3. C 错误：ZK不存储分区日志索引，日志索引文件保存在对应Broker本地磁盘。
4. D 正确：ISR 列表持久化在 ZK，Leader 宕机时 Controller 可从中获取 ISR 成员名单，确保只从与 Leader 保持同步的副本中选举新 Leader，避免选出数据滞后的副本导致数据丢失。
#### 考点总结
ZK存储内容边界、Topic元数据、分区ISR、日志存储位置区分。

#### 题目
同一Topic多分区场景，生产者发送消息丢失、重复问题分析，下列描述不正确的是？  
A. 消息发送后未收到broker ack就触发重试，可能造成分区内消息重复  
B. Topic分区leader宕机，未同步到ISR副本的消息会直接丢失  
C. 生产者通过key哈希固定分区，能保证业务key对应的消息全局有序  
D. Topic分区扩容后，旧分区存量消息不会自动迁移至新分区，新消息按新哈希规则分配  
#### 正确答案
C
#### 简洁解析
1. A 正确：重试机制会产生重复消息，消费端必须幂等处理。
2. B 正确：未同步至ISR的消息无副本，leader故障直接丢失。
3. C 错误：仅能保证**同一分区内有序**，若扩容分区、重分配分区，同key消息可能切换分区，全局有序失效。
4. D 正确：分区扩容不会迁移历史消息，仅新消息重新哈希分配。
#### 考点总结
Topic分区扩容、消息有序性边界、消息丢失重复场景。


#### 题目
关于 Kafka Topic 配置、消息留存与过期清理机制，下列说法正确的是（ ）  
A. Topic 级别设置的 retention.ms 会覆盖 Broker 端 server.properties 中的 log.retention.ms 默认值，但无法覆盖 Broker 动态配置（kafka-configs.sh 修改的配置）   
B. 消息达到留存时间后，Kafka 会以日志段（Segment）为单位删除过期数据，不会逐条删除单条消息  
C. 当磁盘空间紧张时，Kafka 会优先清理未被任何消费者消费的消息，以保证活跃消费不受影响  
D. 同一 Topic 下不同分区的日志文件相互独立，各自单独执行过期清理逻辑   
#### 正确答案
D
#### 简洁解析
1. A 错误：Topic 配置优先级最高，可覆盖 Broker 动态配置与静态配置，后半句描述错误。
2. B 错误：仅单条消息超时不会删除 segment，必须整个日志段所有消息全部过期才会整体删除，表述过于绝对。
3. C 错误：日志清理只依据时间、大小阈值，和消息是否被消费无关，无优先清理未消费消息逻辑。
4. D 正确：每个分区拥有独立日志与 segment，清理线程按分区独立执行过期删除，互不干扰。
#### 考点总结
Topic/Broker配置优先级、Segment删除条件、日志清理规则、多分区存储隔离机制。

#### 考点总结
1. 配置优先级规则：Topic配置 > Broker动态配置 > Broker静态配置；
2. Segment删除前置条件：整段所有消息全部过期才会整体删除，不支持单条消息删除；
3. 日志清理仅依赖时间/大小阈值，和消费者消费位移、消息是否已读取无关；
4. 同一Topic内多分区存储、过期清理逻辑完全隔离独立。

# Redis
### Redis 基本数据类型
#### 题目
常用中间件Redis的基本数据类型不包括？
A. Int  
B. List  
C. String  
D. Hash   

#### 正确答案
**A**

#### 简洁解析
1. A（Int）：Redis没有单独的Int基础类型，数字本质存为String，不属于基础数据类型。
2. B（List）：Redis基础类型，有序链表。
3. C（String）：Redis最基础的数据类型。
4. D（Hash）：Redis基础类型，类似map结构。

#### 考点总结
Redis五大基础类型：String、List、Set、ZSet、Hash，不存在Int类型。

# SQL
### SQL 优化
#### 题目
假设我们要对分布式SQL进行优化，下列方法无效的是？  
A. 可以使用 union all 的情况下，不要使用 union  
B. 两表 join 时，条件字段尽量放在where 里面  
C. 避免关联字段分布倾斜  
D. 大表 join 小表时，可以考虑 map-side join  

#### 正确答案
**B**

#### 简洁解析
1. A 有效：`union` 会去重排序，`union all` 直接拼接无开销，优先使用。
2. B 无效：多表关联过滤条件建议写在 `on` 后，放where会提前过滤影响关联逻辑，无法优化。
3. C 有效：关联字段数据倾斜会造成任务卡死，提前打散可优化执行效率。
4. D 有效：小表广播到Map端做关联，减少Shuffle开销，适合大表关联小表场景。

#### 考点总结
分布式SQL关联查询，过滤条件区分`on`与`where`，关联条件写on更合理。

### Mybatis
#### 题目
对于MyBatis一级缓存说法错误的是？  
A. 同一个SqlSession只能开启一级缓存  
B. 不同的SqlSession，即使同一个Mapper也会到数据库中进行不同的查询，即不同的SqlSession一级缓存是无效的  
C. 在参数与SQL完全一样的情况下并且不声明刷新缓存没超时的，使用同一个SqlSession对象调用同一个Mapper方法时，SqlSession只会取出当前缓存数据，不会再到数据库中进行查询  
D. 在默认没有配置的情况下，只针对同一个SqlSession而言只会开启一级缓存  

#### 正确答案
**A**

#### 简洁解析
1. A 错误：同一个 SqlSession 既可以使用一级缓存（默认开启），也可以访问和提交由 Mapper 管理的二级缓存跨（SqlSession 共享）。
2. B 正确：一级缓存隔离在各自SqlSession，跨Session无法复用缓存，会重新查库。
3. C 正确：同会话、相同SQL参数、缓存未失效，直接读取缓存，不访问数据库。
4. D 正确：MyBatis默认开启一级缓存，作用范围仅限单个SqlSession。

#### 考点总结
MyBatis一级缓存默认开启，作用域为单个SqlSession，每个SqlSession缓存相互独立。  

#### 题目
关于 MyBatis `SqlSessionFactory` 完整构建流程，下列说法错误的是？  
A. 构建核心入口为 SqlSessionFactoryBuilder，通过加载 XML/Java Config 生成 Configuration 全局配置对象  
B. XML 解析阶段会依次解析 properties、settings、typeAliases、mappers，所有Mapper接口与SQL信息存入Configuration  
C. 构建完成后 SqlSessionFactoryBuilder 对象会失去作用，可被GC回收  
D. 一个应用内建议创建多个 SqlSessionFactory 实例，分别对应不同Mapper映射文件提升查询性能  
#### 正确答案  
**D**
#### 简洁解析
1. A 正确：SqlSessionFactoryBuilder 是构建器，解析配置产出 Configuration，再创建工厂。
2. B 正确：XML解析流程固定，所有映射、类型、配置统一存放在单例Configuration。
3. C 正确：Builder仅做一次性构建，工厂创建后无引用，可回收。
4. D 错误：SqlSessionFactory 重量级，全局单例即可；多数据源才需多实例，单纯拆分Mapper无需多个工厂，反而浪费资源。
#### 考点总结
SqlSessionFactory 构建流程、构建器生命周期、工厂单例设计思想。

#### 题目
关于 MyBatis 对象生命周期（SqlSessionFactory、SqlSession、Mapper、Executor）描述正确的是？  
A. SqlSession 是线程安全对象，可在多线程间共享使用  
B. Mapper 代理对象由 SqlSession 创建，生命周期与 SqlSession 绑定，一级缓存随SqlSession销毁清空  
C. Executor 全局单例，所有SqlSession共用同一个Executor  
D. SqlSessionFactory 每次开启会话都会重新创建Configuration配置  
#### 正确答案
**B**
#### 简洁解析
1. A 错误：SqlSession非线程安全，每个请求独立创建、用完关闭。  
2. B 正确：Mapper代理绑定当前SqlSession；一级缓存属于SqlSession，会话关闭缓存清空。  
3. C 错误：每个SqlSession拥有独立Executor，不共享。  
4. D 错误：Configuration全局唯一，工厂创建后不再重复构建。  
#### 考点总结
MyBatis四大核心对象生命周期、线程安全特性、一级缓存归属。  

#### 题目
MyBatis 一级缓存（本地缓存）与二级缓存（系统全局缓存）对比，说法错误的是？  
A. 一级缓存默认开启，作用域为单个SqlSession；二级缓存默认关闭，作用域为同一个Mapper命名空间  
B. 同一SqlSession内多次执行相同查询，未更新数据时命中一级缓存，不访问数据库  
C. 执行insert/update/delete后，只会清空当前SqlSession的一级缓存，不会清空二级缓存  
D. 二级缓存存储的实体类必须实现序列化接口Serializable，否则缓存存储抛出异常  
#### 正确答案
**C**
#### 简洁解析
1. A 正确：一级会话级、二级命名空间级，二级需手动开启。
2. B 正确：一级缓存会话内复用查询结果。
3. C 错误：增删改操作会同时清空当前namespace下的二级缓存，防止脏读。
4. D 正确：二级缓存会序列化对象，实体必须实现序列化。
#### 考点总结
一二级缓存生效范围、更新操作缓存清理规则、序列化要求。

#### 题目
基于MyBatis自定义二级缓存（实现Cache接口），下列描述不正确的是？  
A. 自定义缓存需要实现 org.apache.ibatis.cache.Cache 接口，提供缓存增删查改实现  
B. Mapper中通过 `<cache type="自定义Cache全类名"/>` 指定替换默认缓存  
C. 自定义缓存无法结合MyBatis自带的缓存淘汰策略（LRU/FIFO），只能自行实现过期淘汰  
D. 二级缓存查询时，先查询自定义缓存，未命中才访问数据库，写入结果存入自定义缓存  
#### 正确答案
**C**
#### 简洁解析
1. A 正确：自定义缓存标准实现入口为Cache接口。
2. B 正确：cache标签type属性指定自定义缓存实现类。
3. C 错误：自定义Cache实现可搭配eviction属性复用MyBatis淘汰策略，无需自己实现。
4. D 正确：二级缓存执行逻辑统一，替换存储实现不改变查询流程。
#### 考点总结
自定义二级缓存开发规范、缓存淘汰策略复用机制。

#### 题目
关于MyBatis `<bind>` 标签用法，下列场景与原理说法正确的是？  
A. bind标签仅能拼接字符串，无法模糊查询，模糊匹配必须使用数据库内置concat函数  
B. `<bind name="searchKey" value="'%' + name + '%'"/>` 可预生成模糊参数，避免数据库差异（MySQL/Oracle拼接语法不同）  
C. bind定义的变量仅能在当前 `<select>` 内使用，不能传递到include引用的SQL片段  
D. bind生成的参数会存入二级缓存key，不同bind变量不影响缓存命中  
#### 正确答案
**B**
#### 简洁解析
1. A 错误：bind专门解决跨库模糊拼接，无需数据库concat。
2. B 正确：bind统一封装模糊匹配参数，屏蔽MySQL %、Oracle || 拼接语法差异。
3. C 错误：bind变量作用域包含当前语句内所有include片段。
4. D 错误：bind生成的参数属于查询条件，会参与缓存key计算，值不同缓存无法命中。
#### 考点总结
bind标签作用、跨数据库兼容方案、变量作用域、对二级缓存key的影响。

### WHERE与HAVING的执行时机
#### 题目
关于WHERE与HAVING子句的执行时机、可用过滤字段，下列说法错误的是？  
A. WHERE 在分组 GROUP BY 前过滤原始数据，不能使用聚合函数；HAVING 在分组后过滤分组结果，仅支持聚合字段  
B. 查询语句 `select id,sum(score) s from student where s>60 group by id` 语法合法，可正常执行  
C. HAVING 可以使用SELECT中定义的聚合别名，MySQL支持，标准SQL部分数据库不支持  
D. 若需求为“过滤总分大于300的学生分组”，过滤条件必须写在HAVING，不能写WHERE  
#### 正确答案
**B**
#### 简洁解析
1. A 正确：执行顺序：**FROM→WHERE→GROUP BY→聚合计算→HAVING→ORDER BY**；WHERE无法识别聚合结果。  
2. B 错误：s是聚合别名，WHERE阶段还未完成sum计算，无法识别，直接报语法错误。  
3. C 正确：MySQL解析顺序特殊，允许HAVING引用聚合别名；Oracle等标准数据库不允许。  
4. D 正确：总分是分组聚合后结果，WHERE无法过滤，只能用HAVING。  
#### 考点总结
WHERE与HAVING执行顺序差异，聚合函数不能在WHERE中使用，HAVING专用于分组后过滤。    

#### 题目
数据表 `score`，主键自增 `id`，字段 `class_id`、`score`；需求：统计每个班级平均分大于 80 的班级，按平均分降序。下列 SQL **语法错误**的是？  
A. `SELECT class_id, AVG(score) avg_s FROM score GROUP BY class_id HAVING AVG(score) > 80 ORDER BY avg_s DESC`    
B. `SELECT class_id, AVG(score) avg_s FROM score WHERE score > 0 GROUP BY class_id HAVING avg_s > 80 ORDER BY avg_s DESC`  
C. `SELECT class_id, AVG(score) avg_s FROM score GROUP BY class_id ORDER BY avg_s DESC HAVING avg_s > 80`  
D. `SELECT class_id, AVG(score) avg_s FROM score GROUP BY class_id HAVING avg_s > 80 ORDER BY avg_s DESC`  

#### 正确答案
**C**

#### 简洁解析
1. **A、B、D 正确**：遵循 `GROUP BY → HAVING → ORDER BY` 的语法顺序，可正常执行。
2. **C 错误**：`HAVING` 必须位于 `GROUP BY` 之后、`ORDER BY` 之前；该语句将 `HAVING` 写在 `ORDER BY` 之后，违反 SQL 语法规则，**直接报错**。

#### 考点总结
`GROUP BY`、`HAVING`、`ORDER BY` 的固定书写顺序：`GROUP BY` → `HAVING` → `ORDER BY`，`HAVING` 必须位于分组后、排序前。

#### 题目
关于自增主键、分组HAVING、排序组合场景，描述正确的是？  
A. 自增主键连续无空缺，GROUP BY分组后按自增id排序一定有序，可省去order by  
B. 表删除部分数据后自增主键断裂，不影响HAVING聚合统计结果  
C. HAVING中使用自增主键id做过滤条件，可直接利用主键索引快速过滤分组  
D. 使用自增主键分组，HAVING过滤效率高于普通字段分组，因为主键自带索引  
#### 正确答案
**B**
#### 简洁解析
1. A 错误：分组后结果集打乱，与主键自增顺序无关，必须显式order by。
2. B 正确：自增主键断号只是id数值不连续，聚合统计只看数据行，不受主键空缺影响。
3. C 错误：HAVING过滤的是分组聚合结果，主键索引只作用原始行，无法优化分组后过滤。
4. D 错误：分组效率取决于分组字段索引，主键索引无法优化GROUP BY聚合后的HAVING过滤。
#### 考点总结
自增主键特性，HAVING仅作用分组结果，索引无法优化后置聚合过滤逻辑。

### 索引的生效范围
#### 题目
一张用户表 `user`，主键 `id` 为自增 INT，普通索引 `idx_phone(phone)`，执行 SQL：
`SELECT name FROM user WHERE phone = '13800001111' GROUP BY phone HAVING COUNT(*) > 1 ORDER BY id DESC;`

关于该 SQL 的执行逻辑与优化分析，下列说法正确的是？  
A. `HAVING COUNT(*) > 1` 会在分组前过滤掉不满足条件的记录，从而减少 `GROUP BY` 的分组数据量，提升索引效率  
B. 索引 `idx_phone` 可同时覆盖 `WHERE` 等值过滤和 `GROUP BY` 分组，但 `HAVING` 聚合过滤和 `ORDER BY id DESC` 排序仍会产生临时表或文件排序  
C. 将 `HAVING COUNT(*) > 1` 改写为 `WHERE COUNT(*) > 1`，可在分组前提前过滤数据，避免聚合计算  
D. 主键 `id` 为自增 INT，`GROUP BY phone` 分组后的结果集仍按 `id` 保持有序，因此 `ORDER BY id DESC` 可直接复用主键索引顺序，无需额外排序  

#### 正确答案
**B**

#### 简洁解析  
1. **A 错误**：`HAVING` 的执行顺序在 `GROUP BY` 和聚合计算（`COUNT(*)`）**之后**，只能过滤分组后的结果，**无法减少分组数据量**。  
2. **B 正确**：`idx_phone(phone)` 可覆盖 `WHERE phone = ?` 过滤和 `GROUP BY phone` 分组；但 `HAVING` 聚合过滤和 `ORDER BY id DESC` 排序需要额外临时表/文件排序。
3. **C 错误**：`COUNT(*)` 是聚合函数，`WHERE` 阶段尚未执行分组和聚合，**语法不合法**，无法提前过滤。
4. **D 错误**：`GROUP BY phone` 会按 `phone` 重新组织数据，结果集顺序与 `id` 无关，`ORDER BY id DESC` 需重新排序，**无法直接利用主键索引有序性**。

#### 考点总结
- `HAVING` 与 `WHERE` 执行顺序差异：`WHERE` 过滤原始行 → `GROUP BY` → 聚合计算 → `HAVING` 过滤分组结果
- <font color = 'red'>索引只能优化 `WHERE` 和 `GROUP BY`，无法优化 `HAVING` 聚合过滤</font>
- `GROUP BY` 打乱结果集顺序，`ORDER BY` 需独立排序，不能依赖主键自增特性

#### 题目
表student，主键自增id，唯一索引uk_card(card_no)，联合索引idx_class_score(class,score)；执行语句：  
`SELECT class,MAX(score) max_s FROM student WHERE class IN (1,2) GROUP BY class HAVING MAX(score)>=90 ORDER BY max_s DESC;`下列说法错误的是？  
A. idx_class_score可覆盖where过滤、group by分组、max聚合，减少回表  
B. HAVING基于聚合结果过滤，无法利用索引提前过滤数据  
C. 若删除联合索引，仅保留主键索引，该SQL会产生临时表+文件排序  
D. 将HAVING条件移至WHERE中，可直接走索引过滤，大幅提升性能  
#### 正确答案
**D**
#### 简洁解析
1. A 正确：联合索引前缀class用于筛选分组，score字段支持聚合覆盖。  
   联合索引 idx_class_score (class, score)  
   │                               │  
   ▼                               ▼  
   用于 WHERE class IN (1,2)  用于 MAX(score)  
   用于 GROUP BY class        聚合值直接从索引取
2. B 正确：聚合计算完成后才执行HAVING，索引无法提前拦截不满足聚合条件的数据。
3. C 正确：无联合索引时，分组、聚合、排序都会生成临时文件，性能暴跌。
4. D 错误：MAX(score)是聚合函数，WHERE阶段不存在聚合结果，语法不支持迁移。
#### 考点总结
索引仅优化原始行过滤，HAVING聚合过滤无法下推到WHERE，结合联合索引覆盖考点。


# 微服务
### Eureka自我保护机制
#### 题目
关于Eureka自我保护机制，说法错误的是？  
A. 固定时间内大量实例被注销，可能会严重威胁整个微服务架构的可用性。为了解决这个问题，Eureka 开发了自我保护机制  
B. 自我保护模式正是一种针对网络异常波动的安全保护措施，使用自我保护模式能使Eureka集群更加的健壮、稳定的运行  
C. Eureka Server 在运行期间会去统计心跳失败比例在15分钟之内是否低于 85%，如果低于 85%，Eureka Server 即会进入自我保护机制  
D. 在微服务架构下服务之间通常都是跨进程调用，网络通信往往会面临着各种问题，比如微服务状态正常，网络分区故障，导致此实例被注销  

#### 正确答案
**C**

#### 简洁解析
1. A 正确：大量服务因网络误剔除会引发雪崩，自我保护就是解决该问题。
2. B 正确：应对网络抖动，避免误删健康服务，提升集群稳定性。
3. C 错误：阈值逻辑颠倒。Eureka Server 会统计最近 15 分钟内的心跳续约情况，当续约成功率低于 85%（即失败比例超过 15%）时，才会触发自我保护机制，而不是"失败比例高于 85%"
4. D 正确：网络不通但服务正常时，容易被错误注销实例，是自我保护要解决的场景。

#### 考点总结
Eureka 自我保护机制的完整执行流程
```text
每 1 分钟：
  ├── Renews threshold = (实例总数) × (预期每分钟续约次数)
  ├── Renews count = 过去 1 分钟内实际收到的续约次数
  └── 计算 15 分钟窗口内的累计比例

每 15 分钟检查：
  ├── 如果 (15分钟总续约次数) / (15分钟预期续约次数) >= 0.85
  │   └── 正常模式：允许剔除超时实例
  └── 如果 (15分钟总续约次数) / (15分钟预期续约次数) < 0.85
      └── 进入自我保护模式：**不剔除任何实例**（宁可保留错的服务，也不误杀对的服务）
```

# 架构设计
### 核心概念
#### 1. XSS (跨站脚本攻击, Cross-Site Scripting)

**定义：** 攻击者在网页中注入恶意脚本(通常是JavaScript),当其他用户访问该页面时,浏览器会执行这段脚本,从而窃取用户信息或劫持操作。

**生活化类比(偷听传话):**
这就好比你在公司前台留了一个便签(用户输入),本来写的是"请把快递放门口"。但攻击者偷偷把便签内容改成了"请把快递放门口,并把公司大门密码发短信给我"(恶意脚本)。前台(浏览器)把便签内容贴在公司公告栏上(网页展示),结果每个路过看公告的人(其他用户)都执行了"发密码"的指令。

-   **攻击类型：**
    -   **反射型：** 恶意脚本藏在URL链接里,用户点击链接才触发(例如 `<script>alert('攻击')</script>` 拼在URL参数中)。
    -   **存储型：** 恶意脚本直接存入服务器数据库(如评论区),所有访问者都会中招(危害最大,如多年前的新浪微博蠕虫)。
    -   **DOM型：** 纯前端操作导致,不经过服务器后端处理。
-   **核心危害：** 窃取Cookie/Session(登录态)、劫持用户操作、钓鱼弹窗、获取键盘记录。
-   **核心防御：**
    -   **对输出进行编码(转义)：** 将 `<` 转成 `&lt;`,`"` 转成 `&quot;`。不要相信用户的任何输入,在**数据输出到HTML时**进行过滤。
    -   **使用 CSP (内容安全策略)：** 限制页面只能加载指定域名下的脚本。
    -   **HttpOnly Cookie：** 设置Cookie时增加 `HttpOnly` 属性,禁止JavaScript读取Session Cookie。

---

#### 2. SQL 注入 (SQL Injection)

**定义：** 攻击者利用Web应用程序对用户输入数据过滤不严谨,将恶意的SQL命令"拼接"进查询语句,欺骗数据库执行非预期的指令。

**生活化类比(改试卷答案):**
老师(后端)拿着标准答案(原始SQL)去查学生名单。老师问："请把学号是 '123' 的学生分数给我"。攻击者在提交框里写："**123' OR '1'='1**"。老师把话组合起来变成了："请把学号是 '123' **或者 1等于1** 的学生分数给我"。结果所有学生的分数都被查出来了,甚至攻击者直接加一句 `DROP TABLE students`,把整个成绩表给删了。

-   **核心危害：** 脱库(盗取敏感数据)、删表、提权(通过SQL执行系统命令)。
-   **核心防御：**
    -   **永远不要直接拼接 SQL 字符串。** (这是最根本的)
    -   **使用预编译 (PreparedStatement / 参数化查询)：** 将SQL骨架和参数分开传给数据库,数据库只会把参数当作"数据"处理,不会当作"代码"执行。
    -   **使用 ORM 框架(如 MyBatis/Hibernate)：** 利用框架自带的安全占位符(如 `#{}`)。
    -   **输入校验：** 对特殊字符(如 `'`, `;`, `--`)进行过滤或转义。

---

#### 3. CSRF (跨站请求伪造, Cross-Site Request Forgery)

**定义：** 攻击者诱导已登录的用户(在不知情的情况下)点击一个链接或访问一个恶意网站,浏览器会自动携带该用户在目标网站的有效Cookie,从而伪造用户身份发起恶意请求(如转账、改密码)。

**生活化类比(借你的手盖章):**
你(用户)正在银行的官网(A站)登录着,桌上放着你的私章(Cookie)。小偷(攻击者)发给你一张传单,上面有一个"领取红包"的按钮,你随手点了。这个按钮其实是一个隐藏的请求,要求银行给你转账。因为你的浏览器和银行网站保持着登录状态,银行服务器一看:私章(Cookie)是真的,就会执行转账。**整个过程你并没有主动去操作银行网站,但你"亲手"点了那个按钮,借了你的手盖了章。**

-   **核心危害：** 在用户不知情时执行资金转账、发表恶意评论、修改用户资料(基于用户身份)。
-   **核心防御：**
    -   **加 CSRF Token：** 在表单中埋一个随机生成的Token,服务器校验Token是否匹配。因为攻击者无法获取你页面源码中的Token,所以构造的请求无法通过校验。
    -   **检查 Referer/Origin 字段：** 验证请求是否来自本域页面。
    -   **SameSite Cookie：** 设置Cookie的 `SameSite=Strict` 或 `Lax`,限制第三方网站请求时携带Cookie。

---

#### 总结对比速查表

| 攻击类型 | 攻击对象 | 核心原理 | 一句话防御 |
| :--- | :--- | :--- | :--- |
| **XSS** | 客户端 (浏览器) | 注入恶意脚本,让浏览器执行窃取信息 | **输出转义** + **CSP** + **HttpOnly Cookie** |
| **SQL注入** | 服务端 (数据库) | 拼接SQL,让数据库执行恶意命令 | **预编译 (参数化查询)** |
| **CSRF** | 服务端 (身份认证) | 借用用户已登录的身份偷偷发请求 | **CSRF Token** + **SameSite Cookie** |

---

#### 额外延展

-   **XSS 和 CSRF 的区别：** XSS是**不信任用户输入**,它发生在"页面展示"环节；CSRF是**不信任用户浏览器发起的请求**,它发生在"用户请求"环节。
-   **CSRF 之所以能成功,是因为浏览器自动携带了目标网站的Cookie,而服务器只认Cookie不认人。** 单纯依赖Cookie做身份认证是CSRF的根本原因。

### 分布式
#### 题目  
关于分布式锁实现方案与业务落地问题，下列描述错误的是？  
A. Redis RedLock 存在时钟漂移等理论风险，生产环境多数场景下单节点 Redis + Redisson 可重入锁配合看门狗（Watchdog）机制已能满足需求，无需引入 RedLock 的多节点部署复杂度  
B. 基于数据库唯一索引实现分布式锁，无过期时间机制，极易出现死锁  
C. Zookeeper临时节点分布式锁性能优于Redis，适合高并发秒杀场景  
D. 分布式锁必须配套幂等设计，防止锁失效重试导致重复业务操作  
#### 正确答案
C
#### 简洁解析
1. A 正确：RedLock 依赖多节点时钟同步，存在时钟漂移风险；单节点 Redis + Redisson 可重入锁配合 Watchdog 自动续期，生产环境多数场景下足够稳定，无需引入 RedLock 多节点部署复杂度。
2. B 正确：数据库锁无自动释放机制，服务宕机后锁永久占用，形成死锁。
3. C 错误：ZK 创建/删除临时顺序节点伴随会话维护和节点创建销毁开销，并发吞吐量远低于 Redis，不适合高并发秒杀场景。
4. D 正确：锁超时自动释放后，重试请求可能重复执行业务操作，必须结合幂等设计兜底。
#### 考点总结
分布式锁三种方案性能与缺陷对比、分布式锁与幂等配套设计。


#### 题目
本地锁`synchronized`与分布式锁对比，结合高可靠业务场景，描述不正确的是？  
A. synchronized基于JVM对象头监视器锁，仅作用于单机，集群环境无法保证互斥  
B. synchronized自动释放锁，不存在死锁；分布式锁需设置过期时间避免死锁  
C. 高并发集群下单机synchronized加缓存降级，可完全替代分布式锁保证数据一致性  
D. synchronized是非公平锁，分布式锁可按需实现公平/非公平、可重入锁  
#### 正确答案
C
#### 简洁解析
1. A 正确：本地锁仅约束当前JVM线程，多实例集群会失效。
2. B 正确：代码执行完毕/异常JVM自动释放synchronized；分布式锁依赖过期时间兜底。
3. C 错误：集群多实例同时操作共享资源，单机锁无法互斥，会引发数据错乱，不能替代分布式锁。
4. D 正确：synchronized底层非公平；Redis/ZK分布式锁支持多种锁模式。
#### 考点总结
synchronized本地锁局限、分布式锁设计必要性、集群数据一致性保障。


### 互联网安全
#### 题目
XSS、SQL注入、CSRF三种Web攻击与通用防护手段，说法正确的是？  
A. CSRF攻击利用用户本地存储的Cookie，后端校验Referer/Origin请求头可完全杜绝攻击  
B. 后端仅做参数转义只能防御存储型XSS，无法防御反射型XSS  
C. SQL注入核心成因是字符串拼接SQL，使用MyBatis #{}预编译参数可彻底规避注入风险  
D. 给接口增加Token并放入Cookie，能同时防御XSS与CSRF两类攻击  
#### 正确答案
C
#### 简洁解析
1. A 错误：Referer可被恶意篡改，不能作为唯一防护依据，需搭配独立csrf token。
2. B 错误：统一参数转义、输出编码可同时防护反射型、存储型XSS。
3. C 正确：#{}底层使用PreparedStatement预编译，参数与SQL语句分离，杜绝注入。
4. D 错误：Token存Cookie会被XSS窃取，无法抵御XSS攻击。
#### 考点总结
三大Web攻击原理、后端通用防护方案、防护手段边界。

#### 题目
结合安全防护、降级、幂等、缓存整套架构实践，下列说法错误的是？  
A. 接口流量突增触发服务降级时，仍需保留基础XSS、注入、CSRF安全校验逻辑  
B. 缓存命中率过低时，优先优化热点key、减少大key，再考虑缓存分级管理  
C. 幂等唯一标识建议存入缓存，搭配分布式锁，双重拦截重复请求保障可靠性  
D. 为提升接口访问速度，前端直接跳过Token校验，由后端统一拦截CSRF攻击  
#### 正确答案
D
#### 简洁解析
1. A 正确：降级仅关闭非核心业务，安全校验不能省略，防止攻击趁虚而入。
2. B 正确：热点、大key是缓存命中率低首要诱因，分级缓存作为进阶优化。
3. C 正确：幂等key缓存判重+分布式锁互斥，是高可靠标准实践。
4. D 错误：前端跳过校验会大幅提升后端压力，前后端双层校验是通用安全防护规范。
#### 考点总结
安全防护与降级共存设计、缓存优化路径、幂等+分布式锁可靠实践、前后端协同防护规范。

### 日志监控
#### 题目
系统日志监控体系落地实践，下列架构设计方案存在严重缺陷的是？  
A. 使用日志埋点记录业务耗时、异常堆栈，通过ELK聚合后配置告警规则  
B. 生产环境关闭全量DEBUG日志，仅保留INFO/ERROR，减少磁盘IO与传输压力  
C. 日志仅打印堆栈信息，不携带用户ID、订单号等业务追踪标识  
D. 采用MDC传递链路TraceId，串联全链路日志便于故障定位  
#### 正确答案
C
#### 简洁解析
1. A 正确：日志采集+检索+告警是标准监控链路方案。
2. B 正确：DEBUG日志量大，线上关闭可提升系统吞吐量。
3. C 错误：缺失业务标识无法定位单用户/单笔订单故障，日志失去排查价值。
4. D 正确：TraceId实现全链路日志串联，是日志监控核心实践。
#### 考点总结
日志监控落地规范、链路追踪、线上日志分级策略。
